defaults:
  - data: default
  - model: diffscanauth
  - trainer: default
  - experiment: exp_diffscanauth
  - _self_

project_name: DiffScanAuth
seed: 42

ios:
  outputs_root: outputs

hydra:
  run:
    dir: .
  output_subdir: null
  job:
    chdir: false
