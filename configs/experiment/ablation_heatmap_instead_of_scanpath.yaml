# @package _global_
defaults:
  - override /model: vit_gaze_heatmap

experiment:
  name: ablation_heatmap_instead_of_scanpath
  description: "Replace scanpath supervision with static heatmap supervision"
